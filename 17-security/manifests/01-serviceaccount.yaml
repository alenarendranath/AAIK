# ServiceAccount — dedicated identity for AAIK pods
# Apply: kubectl apply -f 01-serviceaccount.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  name: aaik-sa
  namespace: default
  labels:
    app: aaik
automountServiceAccountToken: false    # opt-in only; mount explicitly per pod
